mutation Login($options: EmailPasswordInput!){
  loginUser(
    options: $options
  ) {
    errors {
      field
      message
    }
    user {
      id
      email
      firstName
      lastName
    }
  }
}